{
  "name": "curate",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ curate",
    "create-local": "graph create --node http://localhost:8020/ curate",
    "remove-local": "graph remove --node http://localhost:8020/ curate",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 curate",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.91.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
